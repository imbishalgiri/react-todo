import React from 'react';

import './AppHeading.styles.css';

const AppHeading = ({heading}) => {

    return(
        <div className="heading__name">{heading}</div>
    )
}

export default AppHeading;
